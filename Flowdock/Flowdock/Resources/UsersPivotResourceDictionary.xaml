<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	
	<DataTemplate x:Key="usersPivotDataTemplate">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>
			<TextBlock Grid.Row="0" Text="Who's here?" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
			<ScrollViewer Grid.Row="1">
				<ItemsControl ItemsSource="{Binding OnlineUsers}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
					<ItemsControl.ItemTemplate>
						<DataTemplate>
							<Border BorderThickness="0 0 0 1">
								<Border.BorderBrush>
									<SolidColorBrush Color="White"/>
								</Border.BorderBrush>
								<Grid>
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="Auto" />
										<ColumnDefinition Width="*" />
									</Grid.ColumnDefinitions>
									<Image Grid.Column="0" Source="{Binding Avatar}" Width="50" Margin="5 0 10 0" />
									<TextBlock Grid.Column="1" Text="{Binding Nick}" Margin="0 20 0 20" HorizontalAlignment="Stretch" TextWrapping="Wrap" />
								</Grid>
							</Border>
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</ItemsControl>
			</ScrollViewer>
		</Grid>
	</DataTemplate>
</ResourceDictionary>